"""Built-in rule packs (YAML).

Not a code package -- it exists so setuptools' package-data reliably ships the
``*.yaml`` rules inside the wheel. See pyproject.toml [tool.setuptools.package-data].
"""
